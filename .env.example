VITE_API_BASE_URL=https://api.xgame.game

# Optional but recommended for production. Leave empty only when no real
# public, non-mutating health endpoint is available.
VITE_HEALTHCHECK_URL=https://api.xgame.game/api/HealthCheck

# Optional override if support SignalR hub moves independently.
VITE_SUPPORT_HUB_URL=https://api.xgame.game/hubs/support
